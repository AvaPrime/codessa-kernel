import {describe,it,expect} from 'vitest';\nimport {evaluate} from '../src/engine.js';\nimport {M0} from './fixtures/M0.js';\nfor(const vector of M0){describe(vector.id,()=>{it('satisfies positive assertions',()=>{const r=evaluate(vector.input); if(vector.expect.tier) expect(r.tier).toBe(vector.expect.tier); if(vector.expect.gate) expect(r.gateResult).toBe(vector.expect.gate); if(vector.expect.mode) expect(r.allowedMode).toBe(vector.expect.mode); if(vector.expect.verification) expect(r.verificationContract?.isExecutable).toBe(true);}); it('satisfies negative assertions',()=>{const r=evaluate(vector.input); if(vector.expect.forbid==='MUTATION_REQUEST') expect(r.decision.type).not.toBe('MUTATION_REQUEST'); if(vector.expect.forbid==='CANONICAL_INFERENCE') expect(r.decision.type).not.toBe('MUTATION_REQUEST'); if(vector.expect.forbid==='TRUST_DECLARED_EFFECT') expect(r.tier).not.toBe('R0');});});}\n