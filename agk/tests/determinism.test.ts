import {describe,it,expect} from 'vitest';\nimport {evaluate} from '../src/engine.js';\nimport {semanticDecisionHash} from '../src/canonical.js';\nimport {M0} from './fixtures/M0.js';\ndescribe('AGK-M0 semantic determinism',()=>{for(const vector of M0){it(`${vector.id} is invariant over 1000 replays`,()=>{const baseline=semanticDecisionHash(evaluate(vector.input)); for(let i=0;i<1000;i++) expect(semanticDecisionHash(evaluate(vector.input))).toBe(baseline);});}});\n