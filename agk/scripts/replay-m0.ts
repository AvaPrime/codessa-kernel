import {evaluate} from '../src/engine.js';\nimport {semanticDecisionHash} from '../src/canonical.js';\nimport {M0} from '../tests/fixtures/M0.js';\nfor(const v of M0){const baseline=semanticDecisionHash(evaluate(v.input)); let failures=0; for(let i=0;i<1000;i++) if(semanticDecisionHash(evaluate(v.input))!==baseline) failures++; console.log(`${v.id}: ${failures===0?'PASS':'FAIL'} baseline=${baseline} variance=${failures}`); if(failures) process.exitCode=1;}\n