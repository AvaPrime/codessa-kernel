# AGK Layer A — v1.0-rc1\n\nThis directory is the executable assurance target for the Analyst Governance Control Kernel.\n\n## Non-negotiable boundary\n\nLayer A is a pure deterministic policy engine. It has no LLM, Notion, GitHub, MCP, network, filesystem-write, wall-clock, randomness, or environment-dependent decision logic.\n\n`evaluate(input)` produces a typed decision. It does not mutate governed state.\n\n## Current milestone\n\n- Specification: `AGK v1.0-rc1` — frozen working contract\n- Milestone: `AGK-M0`\n- M0: `UNVERIFIED` until the generated receipt is independently validated\n- M1: locked until M0 passes\n\n## Execute\n\n```bash\nnpm install\nnpm test\nnpm run build\nnpm run m0\nnpm run receipt:validate\n```\n\nThe M0 runner executes the ten frozen vectors, checks positive and negative assertions, performs adversarial replay, performs 1,000 semantic replays per vector, and emits `AGK-M0-CONFORMANCE-RECEIPT.json` only from execution.\n\n## Important assurance rule\n\nThe implementation must not define its own success criteria. M0 fixtures are maintained separately from the policy engine and are treated as external test authority.\n\nIf an invariant's normative definition cannot be derived from the frozen contract, the harness must fail closed rather than invent semantics.\n